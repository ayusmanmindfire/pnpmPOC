{
  "name": "user-service",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/aws/user-service/src",
  "projectType": "application",
  "tags": ["provider:aws", "domain:users"],
  "targets": {
    "build": {},
    "deploy": {},
    "docker-build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -f Dockerfile --build-arg service=user-service -t user-service-image."
      }
    }
  }
}